{{- if and (eq (include "aiops.managedKeycloakEnabled" .) "true") (eq .Values.postgresql.bootstrapMode "automatic") }}
{{- $bootstrapSecret := required "postgresql.bootstrapExistingSecret is required when bootstrapMode=automatic" .Values.postgresql.bootstrapExistingSecret }}
{{- $runtimeSecret := required "authentication.managedKeycloak.database.runtimeExistingSecret is required" .Values.authentication.managedKeycloak.database.runtimeExistingSecret }}
{{- $host := required "postgresql.host is required for managed Keycloak" .Values.postgresql.host }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "aiops.fullname" . }}-keycloak-db-bootstrap
  labels:
    {{- include "aiops.labels" . | nindent 4 }}
    app.kubernetes.io/component: keycloak-db-bootstrap
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-10"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        {{- include "aiops.labels" . | nindent 8 }}
        app.kubernetes.io/component: keycloak-db-bootstrap
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bootstrap
          image: postgres:17
          imagePullPolicy: IfNotPresent
          command: ["bash", "-c"]
          args:
            - |
{{ .Files.Get "files/bootstrap-keycloak-db.sh" | indent 14 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          env:
            - name: PGHOST
              value: {{ $host | quote }}
            - name: PGPORT
              value: {{ .Values.postgresql.port | quote }}
            - name: PGSSLMODE
              value: {{ .Values.postgresql.sslMode | quote }}
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: {{ $bootstrapSecret }}
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $bootstrapSecret }}
                  key: password
            - name: KEYCLOAK_DB_NAME
              value: {{ .Values.authentication.managedKeycloak.database.name | quote }}
            - name: KEYCLOAK_DB_USER
              value: {{ .Values.authentication.managedKeycloak.database.user | quote }}
            - name: KEYCLOAK_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $runtimeSecret }}
                  key: password
{{- end }}
