# Backend Package Rules

기본 package:

```text
io.strato.aiops
  domain
  application
    port
      in
      out
    service
  adapter
    in
      web
    out
      ai
      crypto
      helm
      kubernetes
      persistence
  config
```

